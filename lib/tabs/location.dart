import 'package:flutter/material.dart';

class Location extends StatelessWidget {
  static String widgetName = "location view";
  const Location({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}