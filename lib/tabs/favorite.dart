import 'package:flutter/material.dart';

class Favorite extends StatelessWidget {
  static String widgetName = "favorite view";
  const Favorite({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}